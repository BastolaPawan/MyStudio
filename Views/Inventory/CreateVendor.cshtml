@model MyStudio.Models.CreateVendorViewModel
@{
    ViewData["Title"] = "Add New Vendor";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h4 class="mb-0">Add New Vendor</h4>
                </div>
                <div class="card-body">
                    <form asp-action="CreateVendor" method="post">
                        @Html.AntiForgeryToken()

                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                        <!-- Vendor Basic Information -->
                        <div class="form-group mb-3">
                            <label asp-for="Name" class="form-label">Vendor Name *</label>
                            <input asp-for="Name" class="form-control" placeholder="e.g., Camera World Ltd." />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label asp-for="ContactPerson" class="form-label">Contact Person</label>
                                    <input asp-for="ContactPerson" class="form-control" placeholder="e.g., John Smith" />
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label asp-for="TaxNumber" class="form-label">Tax Number</label>
                                    <input asp-for="TaxNumber" class="form-control" placeholder="e.g., TXN-001" />
                                </div>
                            </div>
                        </div>

                        <!-- Contact Information -->
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label asp-for="Email" class="form-label">Email Address</label>
                                    <input asp-for="Email" type="email" class="form-control" placeholder="e.g., contact@vendor.com" />
                                    <span asp-validation-for="Email" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label asp-for="Phone" class="form-label">Phone Number</label>
                                    <input asp-for="Phone" class="form-control" placeholder="e.g., +1-555-0101" />
                                    <span asp-validation-for="Phone" class="text-danger"></span>
                                </div>
                            </div>
                        </div>

                        <!-- Address & Terms -->
                        <div class="form-group mb-3">
                            <label asp-for="Address" class="form-label">Address</label>
                            <textarea asp-for="Address" class="form-control" rows="3" placeholder="Full business address..."></textarea>
                        </div>

                        <div class="form-group mb-4">
                            <label asp-for="PaymentTerms" class="form-label">Payment Terms</label>
                            <input asp-for="PaymentTerms" class="form-control" placeholder="e.g., Net 30, 50% advance" />
                        </div>

                        <div class="form-group mt-4">
                            <button type="submit" class="btn btn-success">Create Vendor</button>
                            <a asp-action="Vendors" class="btn btn-secondary">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}